# infra/ingress/README.md
#
# PLACEHOLDER ingress layer (nginx) in front of the gateway.
#
# Not part of the default `docker-compose up` flow yet. Add an ingress profile
# once the gateway routes are finalized and TLS needs are known.
